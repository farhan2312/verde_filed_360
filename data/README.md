# data/

Drop the Verde master-data spreadsheets here before running the import scripts:

- `master-data.xlsx` — stores, farmers, employees (read by `scripts/import-data.ts`)
- `field-options.xlsx` — crops, pests, villages, problem categories, etc.

These files are not committed; the UA Agro originals were removed when this repo was forked.
